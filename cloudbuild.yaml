# JOB STEPS
steps:
  - id: "tf init"
    dir: "terraform"
    args:
      - -x
      - -c
      - terraform init

  - id: "tf apply"
    dir: "terraform"
    args:
      - -x
      - -c
      - terraform apply 