steps:
  - id: 'terraform init'
    name: 'hashicorp/terraform'
    args: ['-chdir', 'terraform', 'init']
  - id: 'terraform apply'
    name: 'hashicorp/terraform'
    args: ['-chdir', 'terraform', 'apply', '--auto-approve']